[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-office"
version = "0.1.0"
description = "File-based orchestrator for Gemini, Codex, Grok Build, and Claude Code collaboration."
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[project.scripts]
agent-office = "agent_office.cli:main"

